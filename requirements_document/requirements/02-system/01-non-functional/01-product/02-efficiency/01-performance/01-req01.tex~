\paragraph{PR001}
  \begin{description}
  \item [Statement] 
    \textit{ The system shall try to connect \gls{conferences} with only two \gls{member}s via peer-to-peer.
			There shall be a server connection if they decide to record or save, so the record or saving will be stored to the server.}
  \item [Priority] \textit{A}
\end{description}